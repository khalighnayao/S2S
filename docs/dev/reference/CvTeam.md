# CvTeam — reference (STUB)

> **📝 STUB — not written yet** (TODO, post-#428/#430 migration). Placeholder so links from the AI-layer
> reference docs (e.g. [`CvTeamAI.md`](CvTeamAI.md)) resolve. A proper reference for the base `CvTeam` engine
> class — per-team state, tech ownership, war/diplomacy state, team-scope accumulators — is wanted, but not
> worth writing while the data model is in flux. For the AI layer in the meantime, see
> [`CvTeamAI.md`](CvTeamAI.md).
