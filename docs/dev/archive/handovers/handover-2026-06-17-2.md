# Session handover — 2026-06-17 (evening): #430 readJson SHADOW validated — units 0/2073, buildings 707→318, group-cap built (NOT deployed)

> **RESUME — re-read the durable docs FIRST; do NOT trust this handover for knowledge.** A handover is a transient
> task-list relay (AGENTS.md). Before touching anything, re-read the #428/#430 spec cluster the README banner lists:
> `data-model-spec`, `enabler-cascade-spec`, `modifier/tally/event` specs, `cascade-engine-430`, `migration-renames`,
> `migration-entity-ranking`, `building-cascade-conversion`, plus **`Tools/Migration/README.md`** (the curator runner
> reference — written this session) and the live code in `Sources/Cascade/`. A stale line loses to a later ruling.

## STATE — everything is UNCOMMITTED on `json-data-migration` (owner builds from the tree + commits)

**⚠ DEPLOY STATE (read first): the group-cap build is in `Build/Release/` but was NOT deployed** — the game was up when
we stopped. The DLL currently in `Assets/` is the prior (HERITAGE-fix) deploy and does **not** have
`cascadeBuildingGroupAllows`. **NEXT step #1 is to deploy it.** The building DATA (tech atoms) IS deployed + live.

## WHAT THIS SESSION DID — validated the DLL-side readJson cascade vs legacy, via `/diagnostic/sweep`

The cascade now reads real JSON and is shadow-validated against the legacy gates with
`GET /diagnostic/sweep?type=units|buildings&player=0` (per-entity cascade verdict vs legacy `canTrain`/`canConstruct`;
returns `{total, agree, diverge, divergences[cap 250]}`). Method: sweep → cluster divergences by cause → fix at the
ROOT (data or a principled cascade addition, never legacy emulation) → re-sweep.

**UNITS: 507 → 0 divergences (2073/2073 exact).** Each fix (details in code comments + the cited spec):
- **spawnOnly** — wildlife/spawn units (legacy `iCost == -1`) → clean `identity.spawnOnly` flag; the cascade gate keys
  on it (replaced the too-broad "no production cost" gate, which wrongly hid settlers). `curate_unit` + cascade.
- **tech-confirm** — unit tech prereqs (`PrereqTech` + `<TechTypes>` AND) → `requires.build.all` TECH atoms
  (enabler-spec §13.8; the migration flattened tech into `enables` and dropped the confirm). `cascadeTechReachable`
  removed from the gate. `curate_unit`.
- **world-unit cap = LIFETIME-CREATED** — `CvCascadeTally::countForCap` reads `getUnitCreatedCount` (a hero "born once,
  does one thing, poofs" still consumes its world slot); the alive tally read 0 → over-offer. Engine owns the counter;
  the tally is the single located seam (owner ruling: leave storage in engine, locate the access).
- **missionary monastery prereq** — the BUILDING atom is satisfied by `hasBuilding` OR special-building-group-waived
  (a civic — State/Free Church waive `SPECIALBUILDING_MONASTERY`) OR obsolete; engine-side resolution, like the BONUS
  `trade|vicinity` resolution. `CvCascadeCondition`.
- **HERITAGE atom domain** — was silently dropped (no domain) → tamed-tapir over-offer; added `hasHeritage` eval.
- **team→empire unit cap** (owner ruling): units have no team cap; `curate_unit.allowed_unit` folds `iMaxTeamInstances`
  → empire. Forward-looking, 0 current data effect.

**BUILDINGS: never-run → 707 → 318, then group-cap wired.**
- **tech-confirm retrofit for buildings** (`curate_building`, same as units) → 707→318. RE-EXPORTED (4610 files) +
  deployed + **LIVE/verified**. Cleared the future-tech wonder over-offers.
- **SpecialBuilding GROUP CAP** (`cascadeBuildingGroupAllows`) — owner model "group-on-member, `allowed(ownGroup, N)`":
  member authors `identity.specialBuildingType` (already migrated), group authors `allowed:{empire:N}` (already
  migrated) → cascade-ONLY fix. Lazy group index (build JSON scan: member→group + group→cap), gate
  `count(group at scope) < N`, coexists with the self-cap. Wired into the sweep + `/diagnostic/canConstruct`. **BUILT
  (FBuild OK), NOT DEPLOYED.** data-model-spec §4.2a.

## NEXT — resume here
1. **DEPLOY + verify the group-cap** — close game → `_Build.ps1 Release deploy` → reload → re-sweep buildings. Expect
   the EU/MA elite-academy over-offers (~28 visible) to clear. (Build is current; only the deploy is owed.)
2. **Remaining building sub-clusters** (each its own dig; characterized this session):
   - **obsoletion-edge mismatch** — COAL_PLANT: legacy runtime `obsoleteTech=TECH_ARMORED_CAVALRY` but XML
     `<ObsoleteTech>=TECH_FUSION`, and `TECH_ARMORED_CAVALRY.obsoletes` has no buildings. Module-merge / inversion dig
     (also smells like a base data bug — "armored cavalry obsoletes the coal plant").
   - **empty-requires special buildings** — relics `RELIC_*`, `CULTURE_*PUNK`, punk SpecialBuildings curate to
     `requires={}` (their tech / culture-bonus prereqs not read). Highest-count cleanest migration dig: find why these
     building types yield empty requires. (e.g. `RELIC_ALHAMBRA` needs `TECH_MIND_UPLOADING`; `CULTURE_CRYSTALPUNK`
     needs `TECH_CRYSTALPUNK` — a real future-era tech, owner OK keeping the content.)
   - **the 707/250-cap left ~57 unenumerated divergences** — re-sweep after the big clusters to surface them.
3. **End-of-migration REVIEW PASS** (owner-called, data-model-spec §7): groups as first-class (unit groups; pedia +
   build-list group-membership display, retire hardcoded `EU`/`MA`/`LL` name prefixes); the SpecialBuilding surface
   (cap + waiver + display); formalize `spawnOnly` + the building-atom-waiver semantics into the specs (currently in
   code comments only).

## UNCOMMITTED CHANGES (owner to inspect + commit; whole `json-data-migration` tree)
This-session deltas on top of the prior plumbing:
- **Migration:** `curate_unit.py`, `curate_building.py`; regenerated `Assets/Data/units` (2016) + `Assets/Data/buildings` (4610).
- **Cascade C++** (in `Sources/Cascade/`, prior-session-created, this-session-modified): `CvCascadeReadJson.{cpp,h}`
  (spawnOnly parse, tech-via-requires, HERITAGE prefix, SPECIALBUILDING folder, group index + `cascadeBuildingGroupAllows`),
  `CvCascadeCondition.{cpp,h}` (HERITAGE domain + eval, building-atom waiver), `CvCascadeEnabler.h` (`spawnOnly`),
  `CvCascadeTally.{cpp,h}` (`countForCap` + `CvGame.h`). Plus `Sources/CvHttpServer.cpp` (group-cap wired into sweep + canConstruct).
- **Docs:** `Tools/Migration/README.md` (NEW — curator runner reference + the `engine.py` clobber trap), `AGENTS.md`
  (trust-but-verify + fix-docs-now rulings), `data-model-spec.md` (team-cap ruling, group-cap §4.2a, groups-first-class §7),
  `building-cascade-conversion.md` (`engine.py` dual-role correction).

## GOTCHAS / TRANSIENT (relay only)
- ⛔ **NEVER run `engine.py --write`** — it is the SUPERSEDED raw emitter and CLOBBERS the entire curated `Assets/Data`
  (raw `iCost`/`requires:null`, ~9500 files). Recovery: `git checkout -- Assets/Data && git clean -fd Assets/Data`. The
  curated runner is **`curate_<entity>.py --write`**. (`Tools/Migration/README.md`.) Hit this clobber this session —
  cause was not re-reading the toolkit docs after the compaction.
- The **buildings sweep (5202 entities)** can exceed the eval-mailbox wait window — the first call may return empty;
  retry. **Pipe `curl` straight to `python`** (Git Bash `/tmp` paths don't resolve for the Windows `python`).
- **Deploy needs the game CLOSED** (sharing violation); `build` (→ `Build/`) is fine while it runs — so compile-check
  with the game up, deploy when it's down.
- Re-export changed-file count vs HEAD is **CUMULATIVE** (all session edits), not per-edit — verify a single edit's
  effect by inspecting diff CONTENT, not the file count.

## DON'T
- Don't let any durable doc depend on this handover; re-derive from the specs.
- Don't add `Cascade/*` to the `.vcxproj` thinking it matters for the build (dead — FastBuild globs the dir).
