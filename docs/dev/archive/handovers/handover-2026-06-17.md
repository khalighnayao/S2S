# Session handover — 2026-06-17 (#430 cascade PLUMBING built in shadow; readJson slice is next)

> **RESUME — re-read the durable docs first; do NOT trust this handover for knowledge.** A handover is a transient
> task-list relay (AGENTS.md: "Handovers are TRANSIENT one-time relays — nothing durable may hinge on one"). Before
> touching anything, re-read what the top-of-file banner in [`../../README.md`](../../README.md) lists: the cascade
> specs in `docs/dev/plans/` (`data-model-spec`, `enabler-cascade-spec`, `modifier-cascade-spec`, `tally-cascade-spec`,
> `event-spine-spec`, `cascade-engine-430`, `migration-renames`, `migration-entity-ranking`,
> `building-cascade-conversion`), `docs/modders/datastructure/` (README = entity *shape*, ORGANIZATION = the
> `Assets/Data` *layout*), `docs/dev/reference/save-load-format.md`, and the live code in `Sources/Cascade/`. Look
> concepts UP; a stale line loses to a later ruling.

## STATE — everything below is UNCOMMITTED on `json-data-migration` (owner builds from the tree + commits)

**Merged to `main` this session:** PR **#441** (#440 Size-Matters split-after-group-merge — `mergeUnits` made
`inhibitSplit` operation-scoped instead of a permanent lock). `main` pulled + merged into the branch; no divergence.

**#430 cascade plumbing — built in shadow, all in `Sources/Cascade/`** (FastBuild globs the dir via `fbuild.bff`
`.UnityInputPath`; the `.vcxproj` is **dead for build** — don't touch it):
- `CvScopedAccumulator` (committed `e8c8e70de`) — additive substrate.
- `CvEventSpine` — `emit(KIND, type, iA, iB, iC)` front door + DOMAIN/DIAGNOSTIC/TRACE OOS firewall + broad logging
  consumer (`Cascade.log` + `/events` tee; resolves building+unit names) + `cascadeRegisterConsumers()`.
- `CvCascadeTally` — multi-domain COUNT machine (buildings + units), per-player→team→world, `count(domain,type,scope,
  ctx)` API, `rebuild()` (§9 seed-from-objects, serializes nothing), `shadowVerify()`. **Shadow-VERIFIED in-game (b=0,
  all scopes).**
- `CvCascadeCondition` — `CvCountAtom` + `all`/`any`/`noneOf` evaluator + `cascadePerValue` + `cascadeWithinAllowed`
  (the count vocabulary over the tally). *Compile-only.*
- `CvCascadeEnabler` — `cascadeBuildable` (requires.build ∧ operate ∧ allowed cap) + `cascadeOperational` (dormancy
  re-check). *Compile-only.*
- `CvCascadeModifier` — flat/percent/multiplier combine slot + `effective(base)`. *Compile-only.*
- `CvCascadeSelfTest` — ⛔ **TEMPORARY** shadow harness (city/unit/tally shadows, gated `gPlayerLogLevel≥1`). **PURGE
  before readJson wiring.**
- Emits: `CvPlayer::changeBuildingCount` + `changeUnitCount` fire DOMAIN events (`iC` = player). `CvGame::doTurn`
  calls `cascadeRegisterConsumers()` + `cascadeSelfTest()`. `BetterBTSAI::streamLogTee` promoted to shared public.

**Verification model (owner ruling):** read-surfaces (tally) are shadow-verified in-game; the rest is compile-verified
and gets a **combined** behavior pass — the plumbing is isolated/mechanical, so per-piece game-runs have little value.
The real gremlins arrive with readJson.

**Durable docs done this session** (read these, don't re-derive): `save-load-format.md` RECOVERED + tally §9
rebuild-on-load; recovered+fact-checked `cascade-modifier-inventory` / `cross-entity-inversion-blueprint` /
`team-buildings` (dropped the superseded #423-bundle docs); **`docs/modders/datastructure/ORGANIZATION.md` NEW** (the
`Assets/Data` layout); AGENTS.md handover-deletability ruling; docs dangling-ref sweep → 0 broken links (base-class
reference docs are intentional STUBS); README index now front-loads the cascade specs.

## NEXT — resume here
1. **Thin `readJson` slice (option B, owner-chosen).** Parse ONE building (`Assets/Data/buildings/<era>/building_*.json`
   — e.g. `building_abu_simbel.json`: `requires.build` with `any`+`disabled` predicate + `allowed:{world:1}`;
   `building_acupuncturists_shop.json`: plain `requires.build.all`) via vendored `Sources/include/picojson.h` into a
   `CvEntityAvailability`, run `cascadeBuildable` for a player, **shadow vs legacy `canConstruct`**. Proves
   parse→machine→shadow on real data. (A standalone `Tools/ReadJson/` harness already parses `Assets/Data`; the
   **DLL-side** readJson is the #430 job.)
2. Then the data-driven assembly: `enables`-family forward index → CAN GET; modifier deposit-flow down the scope spine
   + per-deposit `enabled`/`disabled` + `per`; predicates (`HAS_RIVER`/`IS_CAPITAL`/…). Then full readJson. Then PURGE
   the self-test.

## DEFERRED / OPEN (recorded in the durable docs, not dependent on this handover)
- **`units` era-fold + culture-"unique"-unit dedup** — units flat pending the post-migration culture-unit
  rationalization pass (era is the intended axis; see `ORGANIZATION.md` units note).
- All cascade code + docs above are **uncommitted** — owner commits.

## TRANSIENT POINTS / GOTCHAS THIS SESSION (relay only — not durable facts)
- **Glob whiffs on `dir/**/*.ext` patterns.** The Glob tool repeatedly returned "no files" for valid paths
  (`Assets/Data/**/*.json`, `docs/modders/**/*.md`, `buildings/**/*.json`) while a broad `**/*.json` worked. Cost real
  time locating the data + a doc. Use Bash `ls`/`find` (or broad globs) for directory traversal here.
- **Shadow-log verification has per-`eventId` `b`-semantics — easy to misread (I did, twice).** `eventId=2` (city) →
  `b` is the TRUTH count, match = `a==b`; `eventId=3` (unit) and `eventId=10/11` (tally shadow) → `b` is the MISMATCH
  count, match = `b==0`. Every line now also ends `c=<player>`, which broke a naive `b=0$` anchor. My grep checks
  false-flagged matches as mismatches **twice** — the tally data was correct throughout. Write semantics-aware checks.
- **Deploy fails with `Sharing violation` while the game is running** (the DLL is locked) — close the game before
  `deploy`; the `build` half is unaffected.
- **Assert is slow** (FASSERT + per-turn recompute + the temporary self-test scan). The `[SPINE/*]` logging ships in
  EVERY config (gated, not FASSERT), so FinalRelease verifies the same lines far faster if turn-time bites. (Aside: the
  owner's ~7-8 min FinalRelease build is the optimizer/link cost, **not** the unity-batch count; the 9950X3D ~halved it
  vs the 14900K, and the X3D cache plausibly helps compile working-sets.)
- **`picojson` is vendored** at `Sources/include/picojson.h` (already used by `CvHttpServer`) — the readJson slice uses
  it; nothing to add.
- **Data is on the branch in `Assets/Data/`** (not a git-ignored testOutput) — e.g. `buildings/<era>/building_*.json`.
- A live game was running for much of the session (owner verifying turns); coordinate deploys around it.

## DON'T
- Don't add `Cascade/*` to the `.vcxproj` thinking it matters for the build (dead — Despair Index #14 / AGENTS HARD
  RULE; FastBuild globs the dir). I briefly treated it as a build step this session; it isn't.
- Don't let any durable doc depend on this handover; re-derive from the specs.
