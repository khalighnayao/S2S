# Session handover — 2026-06-16 #8 (DATA PHASE COMPLETE — readJson conformance 326→0; properties first-class; buildRate landed; NEXT = #430 WIRING)

> **Read this to resume.** Self-contained. This session drove the #428 data to **0 conformance flags** against the live
> harness (real `Assets/Data`, 13,442 files), closing the last holes, landing the **buildRate** unification, making
> **properties first-class**, reducing the XML mult/div formula soup to sane fractions, building an **adversarial test
> corpus**, and **relocating the harness `Sources/ReadJson` → `Tools/ReadJson`**. The data/migration phase is now done
> "for now"; **tomorrow flips to the #430 ENGINE WIRING** (the chickens come home to roost — see §NEXT).

> Naming: same-day handovers are NUMBERED. Today: `-16` · `-pm` · `-3` · `-4` · `-5` · `-6` · `-7` · this (`-8`).

---

## ⛔ READ-FIRST GATES (honor every resume)
1. **READ ALL THE DOCS IN FULL before touching anything.** Mandatory: the 5 specs (`data-model-spec.md` canonical ·
   `cascade-engine-430.md` impl plan · `enabler-cascade-spec.md` v0.3 · `modifier-cascade-spec.md` v3 · `tally-cascade-spec.md`),
   `migration-entity-ranking.md`, `migration-renames.md` (the canonical old→new registry — now incl. the "Production vs
   buildRate" + "Property sources → v3" captures), `building-cascade-conversion.md`, and the prior handovers.
2. **OWNER VISUALLY INSPECTS + EXPLICITLY APPROVES before EVERY commit. ASK before any new mechanic/token/shape; WAIT.**
3. **Handovers/plan docs ARE editable** — correct at source when misleading.
4. **Verbose narration** — explain what you're looking for and why, live.

## STATE — on `json-data-migration`. THE DATA IS AT 0 CONFORMANCE FLAGS (13,442 files parse, 0 failed, 0 flags).
This session's rulings + work (all captured in the specs/renames):
- **#1 membership predicates RESOLVED:** `HAS_TERRAIN`/`HAS_FEATURE`/`HAS_BONUS` are canonical single-valued predicates;
  `{terrain|feature|bonus:[…]}` is **authoring SUGAR** → any-of-`HAS_X` (no data churn). `COASTAL_LAND` unused (moot);
  `IS_COASTAL` (city) distinct; bare `HAS_FEATURE` ("has any feature") complementary. (data-model-spec §2.5, enabler §3.)
- **#2/#3:** harness calibration — bare `HAS_FEATURE`, `specialBuildingsWaived` bucket.
- **#4 specialist:** `BuildingInfo.TechSpecialistChanges` is 2D-keyed → `freeSpecialists.city.buildings.{B}.specialists.{S}.flat`
  (a `subTargetType` field on `accumulate_boosts`, config-gated).
- **#5 property sources → v3 + properties FIRST-CLASS:** property is a **yield-like family** (owner: "classed like any
  other yield"); the JSON emits plain modifiers, the ENGINE owns equilibrium (`targetLevel` — +ve education, −ve crime/
  disease). `property_source_v3` now handles `Div(Mult(attr,C),D)` / `Div(attr,D)` → `per` (REDUCED to a sane fraction:
  positive `each`, sign on `value`, lowest terms — the XML mult/div "math-language" was a first-draft fraction hack) and
  `Active` BoolExpr → `enabled` (also property thresholds `Greater(PROPERTY_X,N)` → count atom `min:N+1`). Switched
  handicap/heritage/specialist OFF the rogue raw `clean_property_source`; **mult/div soup fully gone (0 in output)**.
- **buildRate unification:** `production` = TOTAL city output; `buildRate` = "build a TARGET faster" (`buildRate.self`
  bonus-gated, `buildRate.{scope}.{buildings|units|domains}.{T}`, `buildRate.{city,empire}.{military|space}`). **Versailles
  marble fixed** (was `production.city`, now `buildRate.self`). Across building/unit/civic/project/trait.
- **`&`/`|` condition sugar: DEFERRED (maybe)** until the structure's nailed (would reintroduce BoolExpr expr-trees +
  a 2nd condition surface). data-model-spec §2.5.
- **Harness MOVED `Sources/ReadJson` → `Tools/ReadJson`** (it's tooling, not DLL source; rebuilt + verified from there).
  All doc path refs updated.

## THE TEST CORPUS — `Tools/ReadJson/testdata/` (adversarial, validates the parser)
- **valid tree** (the linked "gnarl" web + `BUILDING_GNARL_SINGULARITY`) → **0 flags**. The web centers on `BONUS_GNARLITE`
  to surface bonus weirdness (a bonus must only emit/enable, never RECEIVE a modifier). Includes the autobuilding
  (`BUILDING_EFFECT_GNARL_INFESTATION` + `HERITAGE_TAXON_GNARLIDAE`) exercising the §6.1 grant→auto-build→`requires`-active.
- **invalid tree** (`testdata/invalid/`) → fires the full ~22-flag oracle (every error path). README documents it.
- **Renders prove the modder intent-check:** `readjson.exe --render TYPE` emits plain English ("Temple of Gnarl builds
  faster with gnarlite; +2 happiness; dorms without gnarchy") — grammar errors → flags, semantic errors → the render by eye.
- **MODDER-TOOL vision (owner):** ship this exe so modders validate JSON + get the render ("was this what I meant?").

## DEFERRED BY DESIGN (NOT offenders — the only raw property shapes left)
- **#429 spatial:** feature/improvement `PropertyManipulators` with `RELATION_NEAR` (v3 rejects NEAR) → parked raw in a
  `properties[]` section (intrinsic, not flagged). `clean_property_source` survives ONLY for these + `engine.generic`.
- **#430 outcomes:** the unit/unitcombat `outcomes` (KillOutcomes/Actions, ~170 units carry `GOM_TECH`) — carried raw via
  `engine.generic`, deferred to the #430 outcome-system pass. Not property/formula; not flagged.

## ⛔⛔ NEXT — #430 ENGINE WIRING (data done → code; "all the chickens come home to roost")
0 flags proves the GRAMMAR conforms; it does NOT prove the SEMANTICS survive the engine. Build order
(`cascade-engine-430.md`): **substrate → tally → modifier → enabler**, each in SHADOW alongside the live XML machinery
with gated new-vs-old comparison logs (parity is NOT the metric — the cascade corrects old bugs; triage divergences).
What roosts at wiring (owner-flagged expectations):
- **Rebuild out to the `canDoStuff` gates** — `canConstruct`/`canTrain`/`canEverResearch`/`canDoCivics`/`canFound`/`canCreate`
  (~7 fns + ~37 call sites, enabler §14 demolition map) collapse into ONE generate-then-gate over the shared frontier.
- **New info-class data for everything** — the parsed cascade structures (`enables` edges, `requires` trees, modifier
  deposit records, `grants`). EARLY DECISION: new members on `CvXxxInfo` vs **fresh parallel structures** (plan leans
  fresh — §2b "avoid reusing existing engine code"; rewrite > repurpose, owner). EXE-bound getters (`getNum*`/`get*Info`)
  must resolve at cutover regardless (the one fixed constraint, §3).
- **Properties-as-EXE-free-yields** — the engine must genuinely accumulate ARBITRARY properties + own equilibrium/
  `targetLevel`. The whole first-class claim is unproven until the modifier machine runs.
- **Accumulator math** — combine modes (sum/max/min, cost-asymmetric, multiplier composition), `int×100`, OOS-safe
  integer-only. **Provisionals to re-judge** (Phase-F modifier-ownership): the tech-gated keep-on-building inversions;
  empire-vs-#421-team-scope (the per-city autobuild emulation moves UP later); `per`/`each`; tally current-vs-lifetime.
- **Reuse policy:** build fresh; the ONLY kept pieces = the EXE boundary + `BoolExpr` (the one conditional reuse).

## Pointers
- Specs/registry: as in the READ-FIRST gate. Harness: `Tools/ReadJson/{readjson.cpp, build.ps1, testdata/}` (rebuild via
  `build.ps1`; run `readjson.exe Assets/Data` for the live conformance, `--render TYPE` for clear text). Toolkit:
  `Tools/Migration/` (`store.py`, `engine.py` incl. `property_source_v3`, `boolexpr.py`, `curate_*.py`, `classifications/`).

## Git
Branch `json-data-migration`. This commit = the data-phase landing (0-flag conformance + buildRate + properties-first-class
+ the `Tools/ReadJson` move + corpus + doc captures; 1221 `Assets/Data` files regenerated). **Resume: read the docs IN
FULL → confirm `readjson.exe Assets/Data` is still 0 flags → begin #430 substrate+tally in shadow with gated comparison
logging. Owner inspects/approves; ASK before any new mechanic.**
