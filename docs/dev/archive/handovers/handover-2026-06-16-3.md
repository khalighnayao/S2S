# Session handover — 2026-06-16 #3 (Improvement #22 DONE + art restructure DONE; next = Build #23)

> Naming: same-day handovers are NUMBERED (`-2`, `-3`, …) rather than named day-sections — unbounded, unambiguous
> ordering. Today's sequence: `handover-2026-06-16.md` (#1) · `handover-2026-06-16-pm.md` (#2) · this (#3).

**Read this first to resume.** Self-contained. This session: finished the **art ui/world/sound restructure** (the
prior handover's immediate task), corrected an over-absolute rule, and curated **Improvement #22** end-to-end via a
classify workflow. **Next is Build #23** — the worker-action layer over the now-done Improvement #22 + Feature #21.

---

## ⛔ READ-FIRST GATES (honor every resume — non-negotiable)
1. **READ ALL THE DOCS before touching anything** — look concepts UP, never reconstruct from memory or from the C++
   (it is reworked to fit the data, NOT ground truth; a spec line that conflicts with a later owner ruling is
   stale — the ruling wins). Mandatory, IN FULL:
   - both LOCKED specs: `modifier-cascade-spec.md` (v3) + `enabler-cascade-spec.md` (v0.3)
   - `building-cascade-conversion.md` — "THE MODEL (locked 2026-06-14)" + §0 ontology + "ART BLOCKS … DONE"
   - `migration-entity-ranking.md` — the curation order + the two gates + Phase F
   - `migration-renames.md` — the canonical old→new registry + decisions already made (now has **§Improvement**)
   - the entity's `Tools/Migration/classifications/*.json` (for Build: run the classify workflow first — see below)
   - the prior handovers (this one + `handover-2026-06-16-pm.md` + earlier)
2. **OWNER VISUALLY INSPECTS the written JSON + EXPLICITLY APPROVES before EVERY commit.** Strictly serial, one info
   at a time. PRESENT before committing; **ASK before introducing any mechanic/token/shape** and WAIT for the explicit
   pass (every change incl. corrections). "In the vocab already / follows from a ruling" ≠ pre-approved.
3. **Handovers/plan docs ARE editable (clarified 2026-06-16)** — "rarely needed" ≠ "forbidden"; correct a doc at its
   source when it misleads. "We didn't need to do X before" is not "X must never be done." (See `migration-entity-ranking.md`
   "On HANDOVER MUTABILITY" + memory `docs-editable-dont-harden-observations`.)
4. **Verbose narration (owner ruling)** — explain what you're looking for and why at each step, live.

## STATE — Tier A (12) + Tier B (5) + Tier C (#18–#22) on `json-data-migration`
- **Tier A:** GameSpeed · Handicap · Era · Process · Victory · Vote · CultureLevel · Hurry · BonusClass · CivicOption · Property · Civilization.
- **Tier B:** Tech #13 · Civic #14 · Religion #15 · Corporation #16 · Trait #17.
- **Tier C:** Bonus #18 · Route #19 · Terrain #20 · **Feature #21 DONE** (101 files) · **Improvement #22 DONE** (this session).
- **Working tree clean.** Latest commits (newest first): `cae88b77` Improvement #22 · `ac0fd63e` art-doc reconciliation + handover-rule fix · `c7d75442` art restructure.

### What this session committed
- **`c7d75442` — art restructure:** the 11 bespoke curators (trait/civic/hurry/heritage/victory/religion/corporation/
  specialist/era/project/civilization) now emit top-level **`ui`/`world`/`sound`** (was flat `art`), via shared
  `curate_common.put_art`/`emit_art` + the canonical `ART_BLOCK`. New tokens: `Texture`→`ui.art.texture` (Specialist
  2nd icon), `DefaultPlayerColor`→`world.art.playerColor`, `CreateSound`→`sound.onCompletion`,
  `bFirstSoundtrackFirst`→`sound.introSoundtrack`. cc-curated entities already did this. 869 JSON re-rendered.
- **`ac0fd63e` — docs:** reconciled the remaining stale flat-`art` mentions; corrected the over-absolute "never edit a
  handover" rule (it was a hardened assumption that got over-applied).
- **`cae88b77` — Improvement #22:** 266 JSON + `curate_improvement.py` + `classifications/improvement-classification.json`
  + `mapping/ImprovementInfo.json` fix + renames §Improvement + ranking Phase-F note. Details below.

## ⛔⛔ IMMEDIATE NEXT TASK — Build #23 (the worker action; ≡ Improvement, cleanly separated)
**Build is the action that PLACES improvements AND adds/removes features AND changes terrain AND lays routes/bonuses —
but it is CLEANLY SEPARATED from them (owner 2026-06-16): it references them by FK, never embeds their data.** So Build
is a self-contained action entity sitting OVER the done Improvement #22 + Feature #21. ⚠ The ranking marks Build "✅"
but that is STALE (pre-reset) — **there is NO `curate_build.py`; Build is NOT done.** `mapping/BuildInfo.json` exists
(first-pass filter). 122 base records.

**Approach = the SAME classify workflow as Improvement (it worked well), WITH the size-discipline baked in from the
start** (see the workflow-lesson below). Recon FIRST (map the real BuildInfo data like we did for Improvement), then
classify, then author. The Improvement session is the worked template for every step.

**Recon checklist for Build (do this first, don't trust the mapping):**
- Class: `Sources/Infos/CvBuildInfo.h` (+ `.cpp` read()/getDataMembers). EXE-link: grep `DllExport` (looked ~0 = unconstrained, verify).
- Fields to expect (verify population counts via the store, modules included): `iCost`, `iTime` (build turns),
  `ImprovementType` (FK — the improvement it lays), `RouteType` (FK — builds ALSO lay roads/railroads),
  `PrereqTech`, `PrereqBonusTypes`/`PrereqOrBonuses`, `ObsoleteTech`, `bKill` (consumes the unit?), entry/order,
  **`FeatureStructs`** (per-feature nested: `FeatureType` + `getFeatureTech`/`getFeatureTime`/`getFeatureProduction`/
  `isFeatureRemove` — "build forest" adds FEATURE_FOREST, "chop"/`isFeatureRemove` clears it + yields production),
  **`TerrainStructs`** (terrain change — terraforming builds), `PlaceBonusTypes` (builds that place a bonus).
- Store edges already registered (`store.py`): `BuildInfo.PrereqTech`→builds (tech enables build),
  `PrereqBonusTypes/PrereqBonusType`→builds, `FeatureStructs/.../PrereqTech` + `TerrainStructs/.../PrereqTech`→builds,
  `ObsoleteTech`→builds. So tech `enables`/`obsoletes` the build (store-inverted); the build's own `requires.build`
  is its PrereqBonus + the per-feature/terrain prereq techs (the build-forest tech gate).

**Model considerations for Build (settle WITH the owner before authoring):**
- **The DOUBLE-MAPPING (owner 2026-06-16):** the IMPROVEMENT already self-gates placement (`requires.build`, so events
  can't drop a disallowed improvement, #22). The BUILD gates its OWN action path. They are NOT redundant — two
  placement paths (worker build vs direct event). Build's `requires.build` = its tech/bonus prereqs; the PLOT-placement
  validity (terrain/feature/etc.) lives on the IMPROVEMENT it lays (don't duplicate it onto the build).
- **`setImprovementType` INVARIANT (owner-LOCKED 2026-06-16):** it is a separate NON-CHECKING actor (the doer); the
  CHECK (`canHaveImprovement`→`requires.build`) is the caller's job, run first in the normal path (worker build, events);
  the engine's sanctioned direct ungated calls (raze→ruins, found→city) bypass it by design. Future merge of check+set
  with an explicit skip-check flag → **issue #437**. (Full text in `improvement-classification.json`.)
- **Build places FEATURES (build forest) + TERRAIN (terraform) + ROUTES (roads) + BONUSES** — model each as the build's
  outcome FK + per-outcome tech/time/production. The feature add/remove (`FeatureStruct`) is the build's, gated by its
  own `FeatureTech`. tar/bamboo feature-as-bonus blur = deferred §0 post-migration CONTENT reclassification, NOT #23.
- **PROVISIONAL pending Phase-F:** modifier ownership (up/down, "who brings it") + the tech-modifier gates were set
  before the §6.1 deliveryguy refinement — re-judged in the Phase-F pass, not now (see ranking Phase-F).

## Improvement #22 — how it was done (committed `cae88b77`; the Build template)
A plot-leaf TARGET (Terrain/Feature peer): own `plot`-scope modifiers + a placement `requires.build`; never a source.
Curated from the adversarially-verified **classify-improvement workflow** (4 understand → synthesize → 2 verify →
finalize) → `classifications/improvement-classification.json` (full per-field rationale + every owner ruling lives
there). Key results (also in renames §Improvement):
- Own plot modifiers (un-bury the mapping's identity + `scope:city` trap → `plot`): YieldChanges / RiverSideYieldChange
  (`HAS_RIVER`) / IrrigatedYieldChange (`HAS_IRRIGATION`) / per-bonus YieldChanges (`{HAS_BONUS:bonus}`) →
  `<yield>.plot.flat`; iDefenseModifier → `defense.plot.amount.percent`; iCulture → `culture.plot.flat`;
  iVisibilityChange/iSeeFrom → `vision.plot.{visibilityRange,seeFrom}.flat`.
- **Placement → `requires.build`** (the crux): `all` (tech `{type,scope:team}` + bare plot predicates
  `IS_WATER`/`IS_PEAK`/`IS_FLATLANDS`/`HAS_IRRIGATION`/`HAS_FEATURE`/`HAS_RIVER`/`COASTAL_LAND` + `{natureYield:{}}`),
  `any` (one OR-group make-valid set: `{terrain|feature|bonus:[…]}` + `IS_HILLS`/`IS_FRESHWATER`/`HAS_RIVER`/`IS_PEAK`),
  `noneOf` (`IS_FRESHWATER`). **New tokens owner-approved 2026-06-16** (bare plot predicates + membership atoms +
  `HAS_BONUS:{}` + `{natureYield}`; `IS_RIVERSIDE` rejected as redundant with `HAS_RIVER`; both-ways domain is a
  structural engine rule, NOT an `IS_LAND` atom on every record).
- **Irrigation 3-part:** `bRequiresIrrigation`→`requires HAS_IRRIGATION`; `bCarriesIrrigation`→`identity.carriesIrrigation`
  (KEPT — the improvement must retain carry-ability; propagation is live `updateIrrigated`; team-tech-gated by
  `TECH_CANAL_SYSTEMS`, engine-ANDed, NO tech clause on the improvement).
- **Capability flags → identity** (NOT grants; Terrain `bFound` precedent): incl. `bIsUniversalTradeBonusProvider`→
  `identity.universalBonusTrade` — a **LIVE lynchpin** ("trades ALL bonuses on its plot", via `isImprovementBonusTrade(-1)`
  OR-fold) the workflow wrongly called unwired (it grepped only the dead direct getter — owner caught it). Per-bonus
  `bBonusTrade`/`iDiscoverRand`/`iDepletionRand`→`identity.bonuses.{BONUS}.{…}`.
- Lifecycle (upgrade/pillage) + the `placesBonus/Feature/Terrain` "random spawn" mechanic → identity (spawn mechanic
  needs its OWN pass, OUT OF SCOPE). DROP: TechYieldChanges (→tech, PROVISIONAL), RouteYieldChanges (already on route),
  iHealthPercent (balance-cut), Categories/root iDepletionRand/Button/MapCategoryTypes (dead). PropertyManipulators
  parked (#429).
- ⚑ Tiny open tweaks the owner may revisit: `advancedStart.cost: -1` sentinel on city-ruins (carried faithfully);
  `iCultureRange` home (currently identity).

## ⛔ MODEL LAW locked/clarified this session (all in the repo docs)
- **Art = top-level `ui`/`world`/`sound`** (`art` a SUB-block within ui/world; canonical map `curate_common.ART_BLOCK`;
  shared `put_art`/`emit_art`). DONE for every migrated entity; new entities adopt it natively. (building-cascade-conversion
  "ART BLOCKS … DONE".)
- **New PLOT-STATE predicates** (owner 2026-06-16, bare per enabler-spec §3): `HAS_IRRIGATION`, `IS_FLATLANDS`,
  `IS_HILLS`, `IS_PEAK`, `IS_FRESHWATER`, `IS_WATER`, `HAS_FEATURE`, `COASTAL_LAND` (+ existing `HAS_RIVER`); plus the
  membership atoms `{terrain|feature|bonus:[…]}`, `HAS_BONUS:{}`, and the `{natureYield:{}}` min-threshold atom. The
  plot self-reports each (object-evaluated predicate family).
- **MODIFIER-OWNERSHIP review is a Phase-F task** (owner 2026-06-16): the tech-modifier gates (`Tech*Changes`→tech)
  predate the §6.1 "who brings the modifier" refinement → re-judge up/down ownership in Phase F, carry as-is until then.
- **`setImprovementType` invariant + #437** (above). **Handover/doc mutability** clarified (gate #3 above).
- **WORKFLOW SIZE-DISCIPLINE (lesson this session):** a classify workflow's structured-output agent went off the rails
  *binary-searching the StructuredOutput byte-ceiling with filler* instead of trimming, hanging the run. FIX baked into
  the script: each `finding`/`notes` ≤ 1 tight sentence, cite `file:line` not code dumps, and "if rejected for size,
  SHORTEN and resubmit — NEVER probe the limit." **Bake this into the classify-build workflow from the start.** Also:
  the workflow's grep-only "zero readers" verdict MISSED the indirect-consumer lynchpin (`bIsUniversalTradeBonusProvider`)
  — always sanity-check "unwired" claims (esp. sibling-method / Python consumers) before trusting them.

## Process reminders (memory mirrors the repo; repo is authoritative)
- STRICTLY SERIAL one info at a time; curate → `--write` → **owner inspects the actual JSON** → commit ONE → next.
- "Verify" now = structural adherence to v3/v0.3 + ZERO invention + reading the live C++/Python consumer (no runtime
  test until the atomic cutover). Drop re-check per field vs `Assets/Python` AND intent. EXE-link grep per entity.
- Editing the first-pass `mapping/<Entity>.json` to fix misclassifications is an established pattern (Feature + this
  session's Improvement both did it) — the mapping is a FILTER, not gospel.
- Workflow opt-in: the classify workflow is multi-agent orchestration — only launch with owner go-ahead (owner approved
  it for Improvement; confirm for Build).

## NEXT after Build #23
Tier D: Specialist ✅ · Heritage ✅ · Project ✅ · **PromotionLine → Promotion** (dead-last-ish, rides Promotion) ·
**UnitCombat** (defines the unit-modifier vocabulary) · **LeaderHead** (~90 AI params → `ai`). Tier E monsters:
**SpecialBuilding → Building**, **SpecialUnit → Unit (LAST)**. Then **Phase F** (the HARD GATE: align everything —
predicate modularity, art blocks [done], modifier-ownership review, family names — BEFORE the #430 parser).
Unit note (owner 2026-06-16): units are mostly OUTSIDE the enabler cascade; only PropertyManipulators feed modifiers.

## Pointers
- Specs: `modifier-cascade-spec.md` (v3) · `enabler-cascade-spec.md` (v0.3). Order + Phase F + the two gates:
  `migration-entity-ranking.md`. Renames (has §Improvement now): `migration-renames.md`. Model: `building-cascade-conversion.md`.
- Toolkit: `Tools/Migration/` — `store.py` (module merge + enables-family indexes + EXCLUDED_MODULE_SUBPATHS),
  `engine.py` (YIELDS/COMMERCES/named_array/clean_property_source/property_source_v3), `curate_common.py` (shared core +
  `EntityConfig`/`requires_fn`/`put_art`/`emit_art`/`ART_BLOCK`), per-entity `curate_*.py`,
  `classifications/improvement-classification.json` (the Build template), `mapping/BuildInfo.json` (first-pass).
- Issue filed this session: **Stones2Stars/S2S#437** (merge improvement placement check+set with an explicit skip-check flag).
- Memory (assistant cache; repo authoritative): `docs-editable-dont-harden-observations` (new), `migration-resume-discipline`,
  `serial-info-conversion-only`, `ask-before-new-mechanic`, `migration-data-authoring-principles`, `module-inclusion-case-by-case`.

## Git
Branch `json-data-migration`; everything committed (`cae88b77`), working tree clean. Commit this handover docs-only on
the branch. **Resume: recon BuildInfo → run the classify-build workflow (size-discipline baked in) → present + owner
rules on the open questions → author `curate_build.py` → owner inspects JSON → commit.**
