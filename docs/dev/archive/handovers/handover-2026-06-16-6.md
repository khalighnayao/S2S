# Session handover — 2026-06-16 #6 (TIER E COMPLETE — Building #32 + SpecialBuilding #31 + Unit #34 + SpecialUnit #33 DONE; the #1–34 ranking is CLOSED; next = Tier-G stragglers + Phase F)

> **⏩ UPDATE (end of session):** this handover began at the Building classification boundary; it now covers the WHOLE
> session — **all four Tier-E monsters shipped (Building/SpecialBuilding committed `febd000f`; Unit/SpecialUnit this
> commit).** The #1–34 entity ranking is COMPLETE. Remaining: the **Tier-G unranked stragglers** (CvOutcome et al.,
> ranking "Tier G") + a **BoolExpr/settler follow-up** (below) + **Phase F** (the alignment gate before #430).
>
> Naming: same-day handovers are NUMBERED. Today: `-16.md` (#1) · `-pm.md` (#2) · `-3.md` (#3) · `-4.md` (#4) ·
> `-5.md` (#5) · this (#6).

**Read this first to resume.** Self-contained. This session did the WHOLE of **Building #32 + SpecialBuilding #31** end
to end: an adversarial **classify-building workflow** (288 fields, 14 agents) → collaboratively **shaped a NEW model
primitive with the owner (the `repeatable` grant + `interval`, modifier-spec §4.1)** → authored `curate_building.py`
(bespoke, COVERAGE-CHECK + era foldering) → **wrote 5202 building + 36 special-building JSON (all parse, owner-approved
"a solid start").** Tier E is now down to **SpecialUnit #33 + Unit #34 (LAST)**, then **Phase F**. (The pre-authoring
"IMMEDIATE NEXT — author the curator" section below was updated to the completed state.)

---

## ⛔ READ-FIRST GATES (honor every resume — non-negotiable)

1. **READ ALL THE DOCS before touching anything — IN FULL** (the load-bearing thread usually comes from a doc that
   looked unrelated). Look concepts UP; never reconstruct from memory or the C++ (it is reworked to fit the data, NOT
   ground truth; a spec line conflicting with a later owner ruling is stale — the ruling wins). Mandatory, IN FULL:
   - both LOCKED specs: `modifier-cascade-spec.md` (v3, now incl. **§4.1 repeatable grants + the `interval` primitive**)
     - `enabler-cascade-spec.md` (v0.3)
   - `building-cascade-conversion.md` — "THE MODEL (locked 2026-06-14)" + §0 ontology + the ART/DEFERRED blocks
   - `migration-entity-ranking.md` — the order + the two gates + Phase F (Building = #32)
   - `migration-renames.md` — the canonical old→new registry (Building's §Building gets written WHEN the curator is authored)
   - **`Tools/Migration/classifications/building-classification.json`** — THE per-field analysis for this pass (288
     fields, adversarially verified; the owner rulings below OVERRIDE it where they differ)
   - the prior handovers (this + `-5` + `-4` + `-3` + `-pm` + earlier)
2. **OWNER VISUALLY INSPECTS the written JSON + EXPLICITLY APPROVES before EVERY commit.** Strictly serial. PRESENT
   before committing; **ASK before introducing any mechanic/token/shape** and WAIT for the explicit pass.
3. **Handovers/plan docs ARE editable** — correct a doc at its source when it misleads.
4. **Verbose narration** — explain what you're looking for and why at each step, live.

## STATE — on `json-data-migration` (Building commit `febd000f`; Unit commit this session)

- **THE #1–34 RANKING IS COMPLETE.** All Tier E shipped: SpecialBuilding #31 · Building #32 · SpecialUnit #33 · Unit #34.
- **Unit #34 + SpecialUnit #33:** `Tools/Migration/curate_unit.py` (bespoke; REUSES the §5 vocab; `BASE`/`UNIT_FAMILIES`/
  `VS_KEYED`/`CAP_BOOL`/`VISION_STRUCTS`/`GP_ACTIONS` + `requires_unit` + `pass2` + `curate_special_unit`; coverage-clean)
  → `Assets/Data/units/*.json` (2073) + `Assets/Data/specialunits/*.json` (7), all parse. `SpecialUnitInfo` registered in
  `store.ENTITIES`. Renames §Unit/§SpecialUnit; ranking #33/#34 done.
- **Remaining:** Tier-G stragglers (ranking "Tier G"; CvOutcome first) + the BoolExpr/settler follow-up (below) + Phase F.
- **Building #32 + SpecialBuilding #31 SHIPPED:** `Tools/Migration/curate_building.py` (bespoke; `SCALAR_FAMILIES`/
  `YIELD_FAMILIES`/`COND_KEYED`/`TARGET_KEYED` + `requires_building` + `pass2` + `curate_special`; COVERAGE-CHECK +
  era foldering) → `Assets/Data/buildings/<era>/*.json` (5202) + `Assets/Data/specialbuildings/*.json` (36), all parse.
  Plus `classifications/building-classification.json`, `workflows/classify-building.js`, the **modifier-spec §4.1**
  (repeatable grants + `interval`) + **§8(i)** correction, renames **§Building/§SpecialBuilding**, this handover.

## ⛔ WHAT THIS SESSION SETTLED — owner rulings (these OVERRIDE the classification JSON where they differ)

- **NEW MODEL PRIMITIVE — `grants.repeatable[]` + `interval` (owner-blessed; spec §4.1).** The 3rd grant-lifecycle
  variant (after one-shot `grants` + deferred `outcomes`): fires recurringly on an `interval`, optionally chance-gated.
  - `interval: perTurn` ≡ `{perTurn: 1}`; `{perTurn: N}` = every N turns (bare-string desugar, like §3 predicates).
  - entry = `{ <payload>, interval, chance?, enabled? }`. `chance` reuses the §4 `per` count-scaler (e.g. spawn odds
    ∝ crime). TWO-STAGE: building's own `requires` (outer: is it active) → the entry's `chance`/`enabled` (inner, per
    interval). Engine (§0.6) owns the `getSorenRandNum` roll + barb-owner-selection (negative property → barbarian unit).
  - **Applies to:** `PropertySpawn{Unit,Property}` (the headline: crime→criminal etc.) **+ the generalization the owner
    approved: `iNumUnitFullHeal` + `HealUnitCombatTypes`** (per-turn heal grants, count not chance).
  - **NOT a repeatable grant:** a continuous count-scaled RATE is a `per`-scaled MODIFIER → the **shrine**
    (`GlobalReligionCommerce`) is `commerce.city.flat:{value, per:{type:RELIGION_X, scope:world}}` (per city holding the
    shrine's religion, gold/research/culture) — no new machinery, retires the parked `shrine` block idea.
- **§6.1 DELIVERYGUY on the 22 "inversions" — KEEP-ON-BUILDING keyed by target** (the mapping's `inversionsOut` onto
  Tech/Bonus/Improvement/Terrain is the STALE pre-v3 rule). Improvement/Terrain/Building-keyed deposits stay on the
  building keyed by target; Bonus-gated via `enabled:{type:BONUS_X,scope:city,min:1}`; Tech-gated via
  `enabled:{type:TECH_X,scope:team}` — **tech ones PROVISIONAL pending the Phase-F modifier-ownership review.**
- **`CommerceChangeDoubleTimes`** → a SECOND age-gated deposit `enabled:{existedFor:{min:N}}` (first use of the banked
  predicate, modifier-spec §10).
- **`cityCapture` = its OWN family** (the building's National/Local Capture Probability/Resistance modifiers govern
  capturing CITIES — distinct from the §5 unit `capture` gradient). NOT overloaded onto `capture`.
- **Whole-city capability bools → `identity`** (bNoUnhappiness/bNukeImmune/bQuarantine/bProtectedCulture/
  bNoEnemyPillagingIncome/bForceAllTradeRoutes/…), revisit a dedicated city-capability group in **Phase F**. (Matches
  the Improvement-pass precedent.) EXCEPTIONS that are enables-family: `bForceTeamVoteEligible`→`enables.votes`,
  `Hurrys`→`enables.hurries`, `FoundsCorporation`→`enables.corporations`.
- **DROP (confirmed dead, §8-i):** `iMaxPopulationAllowed`, `iMaxPopulationChange`, `iDCMNukesOkay`/`bDCMNukesOkay`.
- **`iNukeExplosionRand` (meltdown) — NOT emitted.** Live code (`doMeltdown` runs every turn from `doTurn`,
  CvCity.cpp:1353) BUT populated ONLY by the **EXCLUDED** `Bad_Karma/Building_Meltdown` module → no included building
  sets it → curator never emits it (like any all-zero field). Spec §8(i) corrected.

## ⛔ AUTHORING-PHASE rulings (owner, during the curator pass — fold into any reader/Phase-F work)

- **RawVicinity FOLDS into normal `vicinity`** (owner: lose the adjacency strictness). No `rawVicinity` connection kind.
- **`AidRateChanges` / `BonusAidModifiers` → DROPPED** (owner case-by-case): an UNWIRED property "aid" mechanic with NO
  gameplay effect (city arrays allocated+saved but never written-from-building/read-for-effect; only AI-valuation `/3` +
  pedia read the raw building values).
- **`GlobalReligionCommerce` (shrine) + `GlobalCorporationCommerce` (corp HQ) → park the FK** `identity.{shrine,
  corporationHQ}`. BOTH are single enum FKs (NOT per-commerce arrays — getDataMembers `addEnumAsInt`). The per-commerce
  VALUES live on the religion (#15) / corp (#16); the modifier `value × countReligion/CorporationLevels` (world) assembles
  at #430. (I initially mis-read GlobalReligionCommerce as per-commerce — corrected.)
- **`CommerceFlexibles` → `identity.commerceFlexible: [commerce…]`** (a capability: which commerce SLIDERS the building unlocks).
- **`NewCityFree` → `identity.newCityFree` (PARKED), deferred to the UNIT pass** (owner: the settler "carries buildings
  into settling" via the new grant system; the settler-grants-buildings edge is authored on settler-type units at #34).
- **NEW predicate/atom tokens introduced + owner-approved:** `HAS_POWER`, `HAS_STATE_RELIGION`, `STATE_RELIGION_IN_CITY`,
  the tally count kinds `{type:POPULATION|CITY|TEAM|UNIT_LEVEL|AREA_SIZE, min}`, the `{latitude:{min,max}}` atom,
  `enables.votes:["FORCE_TEAM_ELIGIBLE"]`. (Owner: "this works… issues will surface at the reading pass.")
- **Judgment calls (flag if a reviewer differs):** PrereqReligion/Corporation/CultureLevel → `requires.operate` (dormancy,
  the fluid ones); `GlobalBuildingCostModifiers` → a `costs` family (distinct from the intrinsic `cost` section).
- **`loadPrune` is 0** for buildings — they have NO `PrereqGameOption`/`NotGameOption` tags (gated at the module level).

## ⛔⛔ IMMEDIATE NEXT — (a) the BoolExpr/settler follow-up, (b) Tier-G stragglers, (c) Phase F

**(a) BoolExpr/settler follow-up (owner 2026-06-16, a focused shared pass — has a GitHub issue):** build a shared
**BoolExpr → enabler-condition converter** (`Has GOM_TECH X`→a tech atom, `Is TAG_COASTAL`→a `COASTAL` predicate,
`And`/`Or`→`all`/`any`) and use it for: the **SETTLER-grants-buildings** edge (founders `bFound` carry the `NewCityFree`
set "into settling", each building gated by its tech/plot condition — `NewCityFree` is NOT settler-type-based, owner-
verified no settler-type difference yet); the **capital** (`bCapital`→Palace) granted only on the first city
(`empire.cities.max=0`); and retrofitting the PARKED building **`ConstructCondition`** + unit **`TrainCondition`** BoolExprs
(currently listed-in-coverage but NOT parsed into `requires`). One sub-system, done once.
**(b) Tier-G stragglers (ranking "Tier G"):** the unranked gameplay infos — **CvOutcome** (the kill/action defs Unit/
UnitCombat `outcomes` reference — likely FIRST), CvVoteSource, CvInvisible, CvGoody, CvSpawn, CvEvent/EventTrigger,
CvEspionageMission, CvIdea, CvAttachable, CvCommerce/Upkeep, finish CvYield. Triage gameplay-vs-config first.
**(c) Phase F** (the HARD GATE before #430): align every info — predicate modularity, art blocks [done], modifier-ownership
review (incl. the PROVISIONAL tech-gated Building inversions + the base/deposit boundary on Unit), family names.

⚑ **Building follow-ups for #430 / Phase F (not blocking):** the shrine/corpHQ value-assembly (× world count); the
PROVISIONAL tech-gated keep-on-building inversions (§6.1 ownership review); the `requires.operate` vs `build` call on
religion/corp/cultureLevel; `Prereq{Min,Max}Properties` property-band `requires` (parked — verify the CvProperties
sub-object read). Owner: expect issues at the reader pass, "a solid start."

**Method:** author the curator → `--sample <a rich wonder + a basic building + a crime effect-building>` → **owner
inspects the actual JSON** → iterate → `--write` → owner re-inspects → commit ONE (Building+SpecialBuilding+the docs).
Run a COVERAGE CHECK (every XML tag handled by some table — caught misses on Promotion/UnitCombat). Re-check every
"dead" against `Assets/Python` too. Then **SpecialUnit #33 + Unit #34 (LAST)**, then **Phase F**.

## Process reminders (memory mirrors the repo; repo is authoritative)

- STRICTLY SERIAL; owner inspects the actual JSON before commit; ASK before any new mechanic/token/shape.
- Commit via a temp FILE + `git commit -F` (a bash `-m @'…'@` heredoc injects a literal `@`). Verify branch
  (`git branch --show-current` = `json-data-migration`) in the SAME command; run git from the repo ROOT.
- The mapping (`mapping/BuildingInfo.json`) is a FILTER, trusted for NOTHING (owner: "I trust nothing in the building
  data — biggest creative-mechanics target"). The store FILTERS excluded modules (bad_karma/zwip/p2k_multimaps_test),
  so the curator's populated-field set is the TRUE included-data picture (this is how meltdown self-drops).
- Owner is willing to DROP too-insane / extensively-shoehorning mechanics CASE-BY-CASE (relaxes the "drop structure
  not data" rule for Building) — surface drop-candidates, don't force-fit.

## Pointers

- Specs: `modifier-cascade-spec.md` (v3 + §4.1) · `enabler-cascade-spec.md` (v0.3). Order/Phase F/gates:
  `migration-entity-ranking.md`. Renames: `migration-renames.md`. Model: `building-cascade-conversion.md`.
- Toolkit: `Tools/Migration/` — `store.py` (Building edges all wired; EXCLUDED_MODULE_SUBPATHS), `curate_common.py`
  (EntityConfig/curate/apply_channel/accumulate_boosts/requires_fn/post_process/ART_BLOCK/FAMILY_ORDER), `engine.py`
  (YIELDS/COMMERCES/named_array/property_source_v3/clean_property_source), `curate_improvement.py` (the worked template),
  `classifications/building-classification.json` (THIS pass), `workflows/classify-building.js` (the workflow).
- Field inventory: `CvBuildingInfo.cpp` getDataMembers (1666-1923) + the hand-written read() remainder (the comment at
  1633-1665 lists every hand-read field). EXE-link: 0 `DllExport` (verified).

## Git

Branch `json-data-migration`; HEAD `8d810a8c`. Working tree has the uncommitted #6 docs/tooling above (no entity JSON
yet). **Resume: read the docs IN FULL → author `curate_building.py` from `building-classification.json` + the owner
rulings → `--sample` rich buildings → owner inspects JSON → iterate → `--write` → commit (one Building commit), strictly
serial. Carry the open RawVicinity drop-candidate to the owner.**
