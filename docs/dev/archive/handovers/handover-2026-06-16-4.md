# Session handover — 2026-06-16 #4 (Build #23 + PromotionLine #27 + LeaderHead #30 DONE; next = the unit-plane pair Promotion #28 + UnitCombat #29)

> Naming: same-day handovers are NUMBERED. Today: `handover-2026-06-16.md` (#1) · `-pm.md` (#2) · `-3.md` (#3) · this (#4).

**Read this first to resume.** Self-contained. This session committed **three infos** — Build #23, PromotionLine #27,
LeaderHead #30 — filed ticket **#438** (WorldBuilder catastrophic-swap guard), and locked the **object-module
convention** (modifier-spec §0.8). **Next is the heavy unit-plane pair: Promotion #28 + UnitCombat #29**, which
together DEFINE the unit-stat modifier vocabulary (the §5 banked gap) — the fun finale of the unit plane.

---

## ⛔ READ-FIRST GATES (honor every resume — non-negotiable)
1. **READ ALL THE DOCS before touching anything — IN FULL, even the parts that feel unrelated to your entity (owner,
   emphatic 2026-06-16).** This is not box-ticking. The migration is one tightly-coupled model, and the load-bearing
   connection almost always comes from a doc that looked irrelevant to the entity in front of you. **Get the FULL
   picture first** — you cannot tell in advance which thread you'll need. Proof from THIS session alone: curating
   **LeaderHead** (an "AI params" entity) turned on the **Trait** simple/complex split, the **§0.6 producer/creator**
   boundary, and the **WorldBuilder-swap** danger; curating **PromotionLine** (a "grouping list") turned on the
   **property system**, a **purged mod** (Outbreaks-and-Afflictions), and the **buildUp module** convention. None of
   those were predictable from the entity's own fields. If you skim and reconstruct from memory or from the C++, you
   WILL miss the thread and author something subtly wrong. Look concepts UP, never reconstruct from memory or from the
   C++ (it is reworked to fit the data, NOT ground truth; a spec line that conflicts with a later owner ruling is
   stale — the ruling wins). Mandatory, IN FULL:
   - both LOCKED specs: `modifier-cascade-spec.md` (v3, now incl. §0.8 object-module convention) + `enabler-cascade-spec.md` (v0.3)
   - `building-cascade-conversion.md` — "THE MODEL (locked 2026-06-14)" + §0 ontology + the ART/DEFERRED blocks
   - `migration-entity-ranking.md` — the order + the two gates + Phase F
   - `migration-renames.md` — the canonical old→new registry (now has §Build, §PromotionLine, §LeaderHead)
   - the entity's `Tools/Migration/classifications/*.json` (run a classify workflow first ONLY if owner approves — see below)
   - the prior handovers (this + `-3` + `-pm` + earlier)
2. **OWNER VISUALLY INSPECTS the written JSON + EXPLICITLY APPROVES before EVERY commit.** Strictly serial, one info
   at a time. PRESENT before committing; **ASK before introducing any mechanic/token/shape** and WAIT for the explicit
   pass (every change incl. corrections). "In the vocab already / follows from a ruling" ≠ pre-approved.
3. **Handovers/plan docs ARE editable** — correct a doc at its source when it misleads; don't harden a hunch into a
   stated fact (this session: softened an unverified "TB logic is convoluted" claim to "may be complex, verify").
4. **Verbose narration** — explain what you're looking for and why at each step, live.

## STATE — on `json-data-migration` (working tree CLEAN; latest `f4c00a72`)
- **Tier A (12):** GameSpeed · Handicap · Era · Process · Victory · Vote · CultureLevel · Hurry · BonusClass · CivicOption · Property · Civilization.
- **Tier B (5):** Tech #13 · Civic #14 · Religion #15 · Corporation #16 · Trait #17.
- **Tier C (#18–#23):** Bonus · Route · Terrain · Feature · **Improvement #22** · **Build #23 ✅ (this session)**.
- **Tier D:** Specialist #24 · Heritage #25 · Project #26 · **PromotionLine #27 ✅ (this session)** · **Promotion #28 ☐** ·
  **UnitCombat #29 ☐** · **LeaderHead #30 ✅ (this session)**.
- **Tier E (monsters, LAST):** SpecialBuilding→Building #32 ☐ · Building ☐ · SpecialUnit→Unit #34 ☐. Then **Phase F**.

### What this session committed (newest first)
- **`f4c00a72` — LeaderHead #30:** the AI-personality entity (119 records). ~90 params → the unified **`ai`** group,
  granular subgroups (npc/flavours/personality/war/victory/trade/attitude/refuse/memory/contact/noWarProb/unitWeights/
  improvementWeights/favorites). `bNPC`→`ai.npc`; traits→`grants.{traits,developingTraits,complexTraits}` (FAITHFUL —
  mirror deferred, below); `ArtDefineTag`→`world.art.icon`; diplo music→`sound.diplo*`. `LeaderHeadInfo` newly
  registered in `store.ENTITIES`. **The `ai` subgroup vocabulary is PROVISIONAL** (expected rework at the
  load-writing/readJson phase, owner) — captured in renames as the reader's lookup, NOT logged as locked spec.
- **`b0b9e3d5` — PromotionLine #27:** grouping/hierarchy axis for promotions (333 records; re-curated from stale POCO
  placeholders). `loadPrune.onGameOptions`; **`buildUp` = dedicated OBJECT MODULE** (`{active:true}` interim marker, 28
  lines); applicability gates (`unitCombats`/`notOnDomains`/`notOnUnitCombats`) PARKED in identity (deferred to
  Promotion pass). Dropped the stone-dead `PROMOTIONLINE_AFFLICTION_DISEASE_COMMON_COLD` (purged OA-mod vestige; sole
  PropertyType carrier).
- **`c8d41415` — Build #23:** the worker ACTION (304 records). New **`produces`** reserved section (FK outcomes +
  per-outcome tech/time/production); own `requires.build` (tech + connected-bonus); cleanly separated from the
  improvement/feature/terrain/route it references (the DOUBLE-MAPPING). Foldered by primary outcome:
  bonus/forts/routes/features/terraform/improvements/clearing. `EntityEvent`→`world.art.entityEvent` (added to ART_BLOCK).
- **Ticket #438** (Stones2Stars/S2S): WorldBuilder must block option/type swaps that TRULY break the game; the guard is
  DIRECTIONAL (trait-type swaps either way; `SIZE_MATTERS` off→on safe, on→off catastrophic). Captured, not blocking.

## ⛔⛔ IMMEDIATE NEXT — the unit-plane pair (Promotion #28 + UnitCombat #29)
**These are the heavy ones that DEFINE the unit-stat modifier vocabulary** (modifier-spec §5 banked gap:
combat/withdrawal/bombard/air-defense/first-strike/movement/… families). Per the ranking, UnitCombat #29 is the
designated place to "DEFINE the unit modifier vocabulary"; Promotion #28 owns city/plot property modifiers + the
unit-plane self-accumulator. They share the vocabulary, so settle it once and apply to both. Owner ("we will have fun
with the last 2") wants to do them next. Approach: recon FIRST (class + EXE-link + store + consumers), present the
proposed unit-stat family vocabulary + open questions, owner rules, author, owner inspects, commit — strictly serial.
A classify workflow is owner-opt-in (confirm before launching; recon may suffice as it did for Build/PromotionLine/LeaderHead).

### ⚑ Items DEFERRED to the Promotion pass (the landing zone — resolve them there)
- **PromotionLine `buildUp` baselines** (what each build-up line builds up): derive from the line's promotions.
  Surveyed: **4 of 28** build-ups target a PROPERTY (CRIME_FIGHTING/SCHEMES→`PROPERTY_CRIME`, DISEASE_CONTROL→
  `PROPERTY_DISEASE`, TEACH→`PROPERTY_EDUCATION`, via promotion `PropertyManipulators`); the other 24 build up combat
  STATS. Fill `buildUp.property` (4) + a stat identifier (24); the interim `buildUp.active` then drops.
- **PromotionLine applicability gates** (`identity.{unitCombats,notOnDomains,notOnUnitCombats}`): re-home into the
  proper unit-plane enabling vocabulary once defined. ⚑ **WATCH (unverified):** the C++ matching the correct promotion
  line to a unit MAY be complex — verify, don't assume.
- **LeaderHead simple→complex trait MIRROR:** deriving `complexTraits` for the 117 base-only leaders by
  `TRAIT_X`↔`TRAIT_COMPLEX_X` name-match ("developing" = an original with no complex twin). It's a derivation
  depending on the simple↔complex correspondence the Trait pass DROPPED → resolve at the trait-system coding pass (#430).

## ⛔ MODEL LAW locked this session (in the repo docs)
- **OBJECT-MODULE convention (modifier-spec §0.8):** top-level keys are (almost always) **category objects**, not bare
  scalars; a module's object **IS** its boolean — active iff it EXISTS and is **non-empty**; absent/empty ⇒ false (no
  separate `active`/`enabled` flag needed). Don't emit an empty module object as a marker. Worked example: `buildUp`.
- **`produces` reserved section (Build #23):** the worker-action OUTCOME home — FK references (improvement/route/
  terrainChange/featureChange) + per-outcome nested `features[]`/`terraform[]` carrying their own tech/time/production.
  Cleanly separated from the referenced entities (owner: "references by FK, never embeds their data").
- **WB catastrophic-swap guard is DIRECTIONAL (#438):** a transition is catastrophic when it retroactively invalidates
  already-applied live state that can't be reconstructed (trait-type swaps; `SIZE_MATTERS` on→off).
- **`ai` subgroup vocabulary is PROVISIONAL** (owner): expect rework at the load-writing/readJson phase — do NOT lock it
  into the spec; the renames registry carries the reader's old→new lookup.

## Process reminders (memory mirrors the repo; repo is authoritative)
- STRICTLY SERIAL, one info: curate → `--write` → **owner inspects the actual JSON** → commit ONE → next. (Owner often
  wants to SEE the built JSON to judge — build it, don't argue in the abstract.)
- "Verify" = structural adherence to v3/v0.3 + ZERO invention + reading the live C++/Python consumer. Re-check dropped
  fields against `Assets/Python` too (not just C++). EXE-link grep (`DllExport`) per entity.
- Commit messages via a temp FILE + `git commit -F` (a bash `-m @'…'@` heredoc injects a literal `@` — bit us twice).
  Verify branch (`git branch --show-current`) in the same command; run git from the repo ROOT (a stray `cd` in a prior
  python call left the shell in `Tools/Migration` and broke a `git add`).
- Editing the first-pass `mapping/<Entity>.json` / registering a new entity in `store.ENTITIES` is normal (LeaderHead
  needed registering this session).

## Pointers
- Specs: `modifier-cascade-spec.md` (v3, §0.8) · `enabler-cascade-spec.md` (v0.3). Order + Phase F + gates:
  `migration-entity-ranking.md`. Renames (now §Build/§PromotionLine/§LeaderHead): `migration-renames.md`. Model:
  `building-cascade-conversion.md`.
- Toolkit: `Tools/Migration/` — `store.py` (now registers LeaderHeadInfo), `curate_common.py`
  (`ART_BLOCK`+`EntityEvent`, `put_art`/`emit_art`, `FAMILY_ORDER`), per-entity `curate_*.py` (the new
  `curate_build.py` / `curate_promotionline.py` / `curate_leaderhead.py` are worked bespoke templates),
  `classifications/improvement-classification.json` (the heaviest worked example).
- Issue filed this session: **Stones2Stars/S2S#438** (WorldBuilder catastrophic-swap guard).

## Git
Branch `json-data-migration`; everything committed (`f4c00a72`), working tree clean. Commit this handover docs-only on
the branch. **Resume: recon Promotion (#28) [class `CvPromotionInfo` + EXE-link + store + consumers] → propose the
unit-stat family vocabulary + open questions → owner rules → author → owner inspects → commit; then UnitCombat #29.**
